# Auth — never commit
cookies.txt
cookies*.txt
*.curlrc
.env
.env.*
# ...but the placeholder template is safe to publish. NOTE: a trailing '#'
# is NOT a comment in .gitignore, it becomes part of the pattern.
!.env.example

# Downloaded course content + local run state
archive/
content/
downloads/
virtual.inacap.cl/
manifest.json
notebooklm.json
*.log
.lock

# Local tooling metadata (leaks absolute home paths)
.atl/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
